# نقشه‌ی راه بازی جاسوس

وضعیت فعلی: BETA 1.0 — Nuxt 3 + Pinia + TypeScript، موبایل و دسکتاپ، بدون ذخیره‌ی وضعیت. فازهای ۰ و ۱ انجام شد؛ بازی روی دسکتاپ هم قابل اجراست.

مسیرها بعد از فاز ۰: `/` → `/setup` → `/reveal` → `/result`، و `/guide`.
تسک‌ها به ترتیب اولویت چیده شده‌اند؛ هر فاز روی فاز قبلی ساخته می‌شود.

---

## فاز ۰ — زیرساخت و استانداردهای کد

- [x] **T0.1** مهاجرت به TypeScript (`lang="ts"` در کامپوننت‌ها، تایپ `Player` و `Rule` در `types/game.ts`؛ `GameSettings` و `GamePhase` همراه G3.1/G3.2 اضافه می‌شوند)
- [x] **T0.2** نصب و پیکربندی ESLint (`@nuxt/eslint`) + Prettier + اسکریپت‌های `lint` و `typecheck` در `package.json`
- [x] **T0.3** پین کردن نسخه‌ها: `"vue": "latest"` و `"vue-router": "latest"` حذف شوند (Nuxt خودش این‌ها را دارد) — ابزارها با Node 20 و Nuxt 3.13 سازگار پین شدند (ESLint 9، TypeScript 5.6، Vitest 2)
- [x] **T0.4** غیرفعال کردن SSR (`ssr: false`) — بازی کاملاً کلاینتی/آفلاین است؛ این کار هک‌های `onMounted` برای لود تصاویر (`LongBackground`, `ShortBackground`, `Players.vue`) و پرش صفحه‌ی دسکتاپ/موبایل را حذف می‌کند
- [x] **T0.5** اضافه کردن `lang="fa"` و `dir="rtl"` به `<html>` در `nuxt.config.ts` و حذف هک‌های `row-reverse` و `!` برعکس در متن‌ها (`"!شروع بازی"`, `"!پایان بازی"`)
- [x] **T0.6** یکدست کردن نام‌گذاری: `useResolationCheck` → `useDevice`، `gameguid` → `guide`، `CardSelection.vue` → `reveal.vue` (مسیرها همه kebab-case و lowercase)، `components/Views` → `components/game`
- [x] **T0.7** حذف فیلد منسوخ `hid` از meta، حذف `TimeTracking.vue` خالی (یا پیاده‌سازی در فاز ۳)
- [x] **T0.8** `.claude/` به `.gitignore` اضافه شود؛ README واقعی (معرفی، اجرا، ساختار پروژه)
- [x] **T0.9** راه‌اندازی Vitest + `@nuxt/test-utils` برای تست استورها و منطق بازی

## فاز ۱ — سیستم دیزاین (Design System)

- [x] **T1.1** توکن‌ها: بازنویسی `main.css` به لایه‌های `primitive` (رنگ خام) → `semantic` (`--color-bg`, `--color-surface`, `--color-primary`, `--color-danger`, `--color-text`, ...) + توکن‌های `spacing`, `radius`, `font-size`, `shadow`, `z-index`, `duration`
- [x] **T1.2** حذف `font-size: 62.5%` روی root و استفاده از مقیاس تایپوگرافی استاندارد؛ `font-display: swap` برای فونت cinema
- [x] **T1.3** حذف `outline: none` سراسری از reset و افزودن استایل `:focus-visible`
- [x] **T1.4** کامپوننت‌های پایه در `components/ui/` با پیشوند `Base`/`App`:
  - [x] `AppButton` — variantها: `primary | outline | ghost | danger`، سایز، `disabled`، `loading`، هم `to` (لینک) هم `@click` (اکشن)، محتوا از طریق slot (الان `to` اجباری است و نمی‌شود دکمه‌ی عملیاتی ساخت)
  - [x] `AppIconButton` + `AppIcon` (آیکون‌های SVG با `currentColor`، بدون `id` تکراری مثل `Layer_2`)
  - [x] `AppInput` — label، پیام خطا، `maxlength`، حالت focus
  - [x] `AppModal` (`AppBottomSheet` لازم نشد) — با `Teleport`، بستن با Esc و کلیک بیرون، قفل اسکرول، فوکوس‌تراپ
  - [x] `CardBack` / `PlayerCard` / `FlipCard` (به‌جای `AppCard` عمومی) (کارت با انیمیشن چرخش)
  - [x] `AppTabs` — برای صفحه‌ی راهنما
  - [x] `AppCounter` (stepper برای تعداد جاسوس/زمان)
  - [x] `AppConfirm` — دیالوگ تأیید برای اکشن‌های مخرب
- [x] **T1.5** کامپوننت‌های لایه‌بندی: `ScreenLayout` (هدر + بدنه‌ی اسکرول‌شونده + فوتر ثابت برای دکمه) جایگزین `position: fixed` پراکنده در هر صفحه؛ `AppHeader` (لوگو + دکمه‌ی برگشت)
- [x] **T1.6** بازنویسی `LongBackground`/`ShortBackground` به یک `SceneBackground` با prop `variant` و `background-image` در CSS
- [x] **T1.7** استفاده از `100dvh` و `env(safe-area-inset-*)` برای نوار پایین آیفون/اندروید؛ رفع مشکل باکس مشکی که کل صفحه را پر نمی‌کند
- [x] **T1.8** صفحه‌ی نمونه‌ی کامپوننت‌ها (`/dev/ui`، فقط در dev) برای مرور سیستم دیزاین
- [x] **T1.9** کامپوننت `AppCredit`: متن «ساخته شده توسط نوید بنیادی» با لینک به [navidbonyadi.ir](https://navidbonyadi.ir) (`target="_blank"` + `rel="noopener"`) — در صفحه‌ی اصلی (زیر دکمه‌ها)، صفحه‌ی راهنما و صفحه‌ی دسکتاپ؛ کم‌رنگ و غیرمزاحم، بدون تداخل با دکمه‌های ثابت پایین

## فاز ۲ — باگ‌ها و منطق بازی (اولویت بالا)

### ذخیره و بازیابی وضعیت

- [ ] **B2.1 🔴 کرش** — رفرش در `/reveal`: `assignRandomSpy` روی آرایه‌ی خالی اجرا می‌شود → `TypeError: Cannot set properties of undefined (setting 'isSpy')` و صفحه با «نوبت،» بدون نام می‌ماند _(تأیید شده در مرورگر)_
- [ ] **B2.2 🔴** هیچ استیتی ذخیره نمی‌شود؛ با رفرش یا بستن تب همه‌ی بازیکنان و بازی جاری از بین می‌رود → `pinia-plugin-persistedstate` (localStorage) برای بازیکنان، تنظیمات و بازی جاری
- [ ] **B2.3 🔴** نبود گارد مسیر: ورود مستقیم یا رفرش در `/reveal` و `/result` بدون بازیکن/بازی → middleware که بر اساس `phase` بازی به صفحه‌ی درست هدایت کند

### جریان مراحل (قبل/بعد)

- [x] **B2.4 🔴** _(فاز ۱: با `disabled` در `AppButton` حل شد)_ دکمه‌ی «مرحله بعد» با ۰ بازیکن فقط `opacity: 0` می‌شود ولی **هنوز قابل کلیک است** → وارد کارت‌ها می‌شود و کرش می‌کند
- [ ] **B2.5 🔴** حداقل تعداد بازیکن وجود ندارد (با ۱ نفر بازی شروع می‌شود) → حداقل ۳ بازیکن
- [ ] **B2.6 🔴** در صفحه‌ی کارت‌ها دکمه‌ی «شروع» از اول فعال است؛ می‌شود قبل از دیدن کارت همه‌ی بازیکنان رد شد → فقط بعد از آخرین بازیکن فعال شود
- [ ] **B2.7 🔴** کارت آخرین بازیکن بعد از کلیک بسته نمی‌شود (شاخه‌ی `else` خالی در `turnBackCard`) و کلمه روی صفحه می‌ماند تا بقیه ببینند
- [ ] **B2.8 🔴** برگشت (دکمه‌ی back یا back مرورگر) از `/result` به `/reveal` یا رفت‌وبرگشت → `onMounted` دوباره اجرا می‌شود و **کلمه و جاسوس دوباره قرعه‌کشی می‌شوند** در حالی که بعضی‌ها کارت قبلی را دیده‌اند. قرعه‌کشی باید یک بار در اکشن `startGame()` استور انجام شود، نه در `onMounted` صفحه
- [ ] **B2.9** دکمه‌ی برگشت در صفحه‌ی کارت‌ها وسط بازی بدون هشدار خارج می‌شود → دیالوگ تأیید «بازی لغو شود؟»
- [ ] **B2.10** دکمه‌ی «پایان بازی» همه‌ی بازیکنان را پاک می‌کند؛ برای دور بعد باید اسم‌ها را دوباره وارد کرد → جدا کردن «دور جدید (همین بازیکنان)» از «بازی جدید»
- [x] **B2.11** _(فاز ۱: `AppButton` حالا `click` را emit می‌کند؛ پایان بازی با `navigateTo`)_ `@click="resetData"` روی `Button` که `emit` ندارد؛ فقط به‌خاطر fallthrough روی `NuxtLink` کار می‌کند → اکشن در استور + `navigateTo`

### ورودی بازیکنان

- [ ] **B2.12** _(نام خالی/فقط فاصله در فاز ۱ رد می‌شود؛ پیام خطا مانده)_ نام خالی/فقط فاصله قبول می‌شود (`required` فاصله را رد نمی‌کند) → `trim` و اعتبارسنجی
- [ ] **B2.13** نام تکراری مجاز است → پیام خطا
- [ ] **B2.14** طول نام محدود نیست و کارت را خراب می‌کند → `maxlength` (مثلاً ۱۲) + `text-overflow: ellipsis`
- [ ] **B2.15** سقف تعداد بازیکن وجود ندارد (مثلاً ۲۰)
- [ ] **B2.16** بعد از ثبت نام، مودال بسته می‌شود → برای افزودن سریع چند نفر باز بماند، فوکوس خودکار روی input، دکمه‌ی «تمام»
- [ ] **B2.17** _(ناحیه‌ی لمس ۴۴px در فاز ۱ درست شد؛ تأیید حذف مانده)_ حذف بازیکن با یک لمس بدون تأیید؛ دکمه‌ی `x` کوچک است (زیر ۴۴px)

### کلمات

- [ ] **B2.18** کلمات تکراری در لیست: `زیتون`، `پسته`، `کچاپ`، `نمک`، `فلفل` (هر کدام ۲ بار)
- [ ] **B2.19** ممکن است کلمه‌ی دور قبل دوباره بیاید → نگه‌داشتن تاریخچه‌ی کلمات استفاده‌شده
- [ ] **B2.20** انتقال کلمات از استور به فایل داده‌ی جدا (`data/words.ts`) با دسته‌بندی (مکان، خوراکی، شیء، ...)

### سایر

- [x] **B2.21** _(در فاز ۰ با `composables/useDevice.ts` حل شد)_ `onMounted`/`onUnmounted` داخل استور Pinia (`useResolationCheck`) — به کامپوننتی که اول استور را ساخته وصل می‌شود؛ باید composable باشد (`useMediaQuery` از VueUse)
- [x] **B2.22** _(در فاز ۰ حل شد)_ در دسکتاپ `<NuxtPage />` با `display:none` باز هم رندر و mount می‌شود → `v-if` به‌جای کلاس `hidden`
- [x] **B2.23** _(فاز ۱: بازی روی تبلت/دسکتاپ داخل قاب گوشی وسط صفحه اجرا می‌شود + QR و اعتبار در کنار)_ مرز ۴۵۰px موبایل را هم نمی‌گیرد (گوشی‌های بزرگ در حالت افقی، تبلت) → به‌جای بلاک کردن، لایه‌ی حداکثر عرض وسط صفحه
- [ ] **B2.24** `v-for` بدون `:key` در همه‌ی صفحات (`Players.vue`, `spyidentity.vue`, `gameguid.vue`) → key بر اساس `id` یکتای بازیکن، نه index _(فعلاً key موقت با index اضافه شد)_
- [x] **B2.25** _(فاز ۱: قوانین به `data/rules.ts` منتقل شد، تب فعال state محلی صفحه است)_ وضعیت تب فعال راهنما (`active`) داخل استور و داده‌ی ثابت قوانین نگه داشته می‌شود → state محلی صفحه؛ قوانین به فایل داده
- [x] **B2.26** _(فاز ۱: با `AppTabs` حل شد)_ `v-if` داخل `v-for` در راهنما سه div خالی می‌سازد
- [x] **B2.27** _(در فاز ۰ حل شد)_ `import { defineProps } from "vue"` در `Button.vue` (macro است و نیاز به import ندارد → warning)
- [ ] **B2.28** دکمه‌ی «متوجه شدم» در راهنما همیشه به `/` می‌رود؛ اگر از وسط بازی باز شده باشد باید به صفحه‌ی قبل برگردد
- [x] **B2.29** _(فاز ۱: دکمه‌ها `button`/`a` واقعی، `aria-label` برای آیکون‌ها، `alt` برای لوگو و QR، فوکوس‌تراپ مودال)_ دسترس‌پذیری: `div`های قابل کلیک → `button`، `alt` خالی تصاویر، `aria-label` برای آیکون‌ها

## فاز ۳ — تکمیل گیم‌پلی

- [ ] **G3.1** مدل مراحل بازی در استور `useGame`: `setup → reveal → discussion → voting → result` با `phase` ذخیره‌شده
- [ ] **G3.2** صفحه‌ی تنظیمات بازی: تعداد جاسوس (۱ تا n/3)، زمان دور، دسته‌بندی کلمات
- [ ] **G3.3** صفحه‌ی «رد کردن گوشی»: بین هر دو بازیکن «گوشی را به X بده» تا بازیکن بعدی کارت قبلی را نبیند؛ نمایش پیشرفت (۳ از ۷)
- [ ] **G3.4** تایمر بحث (جایگزین `TimeTracking.vue` خالی): شمارش معکوس، توقف/ادامه، هشدار صدا/لرزش پایان زمان، کار کردن در پس‌زمینه بر اساس timestamp نه `setInterval`
- [ ] **G3.5** شروع‌کننده‌ی تصادفی سؤال‌پرسیدن
- [ ] **G3.6** مرحله‌ی رأی‌گیری و نمایش نتیجه (برد جاسوس/شهروندان) + فرصت حدس کلمه برای جاسوس
- [ ] **G3.7** دکمه‌ی «جاسوس کیه؟» با تأیید و فقط بعد از پایان زمان/رأی‌گیری
- [ ] **G3.8** امتیازدهی بین دورها و جدول امتیاز
- [ ] **G3.9** راهنمای کامل‌تر (مراحل، مثال، نکات)

## فاز ۴ — کیفیت و انتشار

- [ ] **Q4.1** تست واحد برای استورها (قرعه‌کشی جاسوس، اعتبارسنجی نام، انتقال فازها، عدم تکرار کلمه)
- [ ] **Q4.2** تست E2E (Playwright) برای یک دور کامل بازی + سناریوی رفرش وسط بازی
- [ ] **Q4.3** PWA (`@vite-pwa/nuxt`): نصب روی گوشی، اجرای آفلاین، آیکون و splash
- [ ] **Q4.4** انیمیشن‌ها: چرخش کارت، ترنزیشن بین صفحات، احترام به `prefers-reduced-motion`
- [ ] **Q4.5** بهینه‌سازی asset: `card-back.svg` و `spy-qrcode.svg` (هر کدام ~۳۳KB) و فونت ۷۰KB → فشرده‌سازی/woff2
- [ ] **Q4.6** CI در GitHub Actions: lint + typecheck + test + build
- [ ] **Q4.7** دیپلوی (مثلاً Netlify/Vercel/GitHub Pages با `nuxt generate`) و به‌روزرسانی QR code به آدرس نهایی
